---
"toolchains-init": patch
---

Preserve existing package.json fields and dependency versions when running the TanStack Router initializer, keeping only newly added dependencies from the official CLI output.
