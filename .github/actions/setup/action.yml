name: Setup
description: Install tools from mise.toml and Yarn dependencies.

runs:
  using: composite
  steps:
    - name: Setup mise tools
      uses: jdx/mise-action@v4

    - name: Enable Corepack
      shell: bash
      run: corepack enable

    - name: Install dependencies
      shell: bash
      run: yarn install
